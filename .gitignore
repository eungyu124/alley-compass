# ── OS / Editor ─────────────────────────────────────
.DS_Store
.idea/
*.swp

# ── Secrets (NEVER commit) ──────────────────────────
.env
.env.*
!.env.example

# ── Python ──────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.ipynb_checkpoints/

# ── ETL 데이터 (파이프라인이 재생성) ────────────────
alley_compass_etl/data/raw/
alley_compass_etl/data/processed/

# ── 로컬 도구 상태 ──────────────────────────────────
.claude/

# ── Web (React/Vite) ────────────────────────────────
node_modules/
dist/
*.local

# ── ML 산출물 (재현 가능, 용량 문제로 커밋 안 함) ────
ml/models/*.joblib
